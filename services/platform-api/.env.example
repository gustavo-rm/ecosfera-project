# platform-api — variáveis de ambiente locais.
# cp services/platform-api/.env.example services/platform-api/.env
#
# Requer a infra de apoio no ar (ver infra/compose/dev.yml).
# O schema relacional deste serviço é "platform" (ver src/infrastructure/database/data-source.ts).

DATABASE_URL=postgresql://ecosfera:change-me@localhost:5432/ecosfera
DB_LOGGING=false

PORT=3000
